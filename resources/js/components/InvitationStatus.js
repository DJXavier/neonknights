import axios from 'axios';
import React, { Component } from 'react';
import ReactDOM from 'react-dom';

class InvitationStatus extends Component {
    constructor(props){
        super(props);
        this.state = {
        }
    }

    render() {
        if ()

        return {

        }
    }
}

if (document.getElementById('invitationStatus')) {
    let entryElement = document.getElementById('invitationStatus');
    ReactDOM.render(<InvitationStatus />, entryElement)